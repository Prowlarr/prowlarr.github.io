<if(input.icon)>
  <if(input.icon.startsWith("./"))>
    <img style="width:1em;height:1em" src=`${input.icon.substring(2)}` class=`icon${input.icon.endsWith(".svg") ? " svg" : ""}`/>
  </if>
  <else>
    <i class=`${input.icon.replace("-", " fa-")}`/>
  </else>
</if>
